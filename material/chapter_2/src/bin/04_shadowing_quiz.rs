//! 연습문제 4: 섀도잉 (Shadowing)
//!
//! 아래 코드의 빈칸(/* TODO */)을 채워서 프로그램을 완성하세요.
//! 실행 방법: cargo run --bin 04_shadowing_quiz
//! 정답 확인: cargo run --bin 04_shadowing

fn main() {
    // =====================
    // 문제 1: 기본 섀도잉
    // x를 5로 선언한 후, 섀도잉으로 10으로 재선언하세요.
    // 기대 출력: "x = 10"
    // =====================
    let x = 5;
    /* TODO: x를 10으로 섀도잉하세요 */
    // println!("x = {}", x);

    // =====================
    // 문제 2: 타입 변경 섀도잉
    // spaces를 문자열 "     " (공백 5개)로 선언한 후,
    // 섀도잉으로 그 길이(usize)로 변경하세요.
    // 기대 출력: "spaces의 길이: 5"
    // =====================
    let spaces = "     ";
    /* TODO: spaces를 문자열의 길이로 섀도잉하세요 (힌트: .len() 메서드) */
    // println!("spaces의 길이: {}", spaces);

    // =====================
    // 문제 3: 연산을 통한 섀도잉
    // number를 2로 선언하고, 섀도잉으로 2배씩 증가시키세요.
    // 기대 출력: "최종 number: 8"
    // (2 -> 4 -> 8)
    // =====================
    let number = 2;
    /* TODO: number를 2배로 섀도잉하세요 (첫 번째) */
    /* TODO: number를 2배로 섀도잉하세요 (두 번째) */
    // println!("최종 number: {}", number);

    // =====================
    // 문제 4: 스코프 내 섀도잉
    // outer를 100으로 선언하고, 내부 스코프에서 200으로 섀도잉하세요.
    // 내부 스코프 출력: "inner: 200"
    // 외부 스코프 출력: "outer: 100"
    // =====================
    let outer = 100;
    {
        /* TODO: outer를 200으로 섀도잉하세요 */
        // println!("inner: {}", outer);
    }
    // println!("outer: {}", outer);

    // =====================
    // 문제 5: 문자열 -> 숫자 변환 섀도잉
    // input을 문자열 "42"로 선언한 후,
    // 섀도잉으로 정수 42로 변환하세요.
    // (힌트: .parse::<i32>().unwrap() 사용)
    // 기대 출력: "변환된 값: 42"
    // =====================
    let input = "42";
    /* TODO: input을 정수로 파싱하여 섀도잉하세요 */
    // println!("변환된 값: {}", input);
}
