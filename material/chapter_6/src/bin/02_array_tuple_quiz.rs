//! 배열과 튜플 - 퀴즈
//!
//! 아래 코드의 빈칸(/* TODO */)을 채워서 프로그램을 완성하세요.

fn main() {
    // 문제 1: 크기가 4인 i32 배열을 선언하고 [10, 20, 30, 40]으로 초기화하세요
    /* TODO: 타입 명시 */ 
    /* TODO: 배열 초기화 */
    let scores: [i32; 4] = [10, 20, 30, 40];
    println!("문제 1 결과: {:?}", scores); // [10, 20, 30, 40]

    // 문제 2: 튜플에서 두 번째 요소(나이)를 추출하세요
    let person: (&str, i32, &str) = ("김철수", 30, "서울");
    /* TODO: 튜플의 두 번째 요소 접근 */
    let age = person.1;
    println!("문제 2 결과: 나이는 {}세입니다.", age); // 30

    // 문제 3: 구조분해를 사용하여 튜플의 모든 값을 변수에 할당하세요
    let point: (f64, f64) = (3.5, 7.2);
    /* TODO */ 
    /* TODO */
    let (x, y): (f64, f64) = point;
    println!("문제 3 결과: x={}, y={}", x, y); // x=3.5, y=7.2

    // 문제 4: 배열의 길이를 구하세요
    let numbers: [i32; 5] = [1, 2, 3, 4, 5];
    /* TODO: 배열 길이 구하기 */
    let length: usize = numbers.len();
    println!("문제 4 결과: 배열 길이 = {}", length); // 5

    // 문제 5: 동일한 값 7로 채워진 크기 3의 배열을 생성하세요
    /* TODO: [값; 크기] 형태 사용 */
    let sevens: [i32; 3] = [7; 3];
    println!("문제 5 결과: {:?}", sevens); // [7, 7, 7]

    // 문제 6: 튜플을 반환하는 함수를 완성하세요
    /* TODO: 반환 타입 (i32, i32) */
    /* TODO: (최솟값, 최댓값) 튜플 반환 */
    /* TODO: (최솟값, 최댓값) 튜플 반환 */
    fn min_max(a: i32, b: i32) -> (i32, i32) {
        if a < b {
            (a, b)
        } else {
            (b, a)
        }
    }
    let (min, max) = min_max(10, 5);
    println!("문제 6 결과: min={}, max={}", min, max); // min=5, max=10
}
